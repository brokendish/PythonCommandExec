単にコマンドを入力して、「選択した範囲」のコマンドを実行し（ここがみそ）、結果を表示するという物を作ってみた。
（数年前に作っていたものですが、少し改良しました）
「選択した範囲」のコマンドを実行することで、パイプ処理の途中経過を見ながらコマンドの組み合わせを作れる。
例えば、「cat gui_python.py | grep import | awk ‘{print $2}’」を見ても結果がイメージできない場合、パイプ「|」の前までを選択して
実行することでどのような結果を次のパイプ渡しているのかがイメージ出来る。


実行方法：
./command_exec.py
